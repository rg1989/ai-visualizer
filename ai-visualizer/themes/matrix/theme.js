/* matrix -- unfinished pair: selectable so it is never forgotten, default
 * palette until it gets its polish pass one day.
 *
 * A theme folder: see themes/README.md for the contract.
 */
AVTheme.add("matrix", { label: "MATRIX", face: "rain", wip: true, css: {}, canvas: {} });
